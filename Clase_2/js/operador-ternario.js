'use strict';

// Condicional ternario

// * Este operador es una forma resumida de poder evaluar el cumplimiento o no de una condición.

// * Es muy útil para ahorrarnos código, pero es importante entender el funcionamiento de los condicionales para poder llevarlo a la práctica

// * Este operador lo vamos a utilizar con mayor profundidad cuando por ejemplo queramos asignar clases mediante javascript a nuestros elementos en el html, para cambiar un color, un background, etc

let edad, estudia;
    edad    = 20;
    estudia = true;

    (edad >= 18 ) ? 'Es mayor de edad' : 'Es menor de edad';

